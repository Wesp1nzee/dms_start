# Платформа документооборота для юристов

Frontend-only решение для управления документами, договорами и делами. Все данные хранятся локально в IndexedDB. Приложение полностью автономно и готово к подключению реального backend.

## Возможности

- 📄 **Управление документами**: создание, редактирование, поиск, фильтрация
- 📋 **Шаблоны с merge-полями**: WYSIWYG редактор, автоматическое заполнение {{field}}
- 📖 **Версионирование**: полная история изменений, откат на любую версию
- 💬 **Комментарии**: система комментариев к документам
- 🔍 **OCR распознавание**: распознавание текста из PDF/изображений (mock + Tesseract.js)
- 🏷️ **Метаданные**: номер дела, номер договора, контрагент, сумма, тип экспертизы
- 📂 **Черновики и статусы**: draft, active, archived, completed
- 💾 **Экспорт/импорт**: резервные копии БД в JSON
- 👥 **Справочники**: управление контрагентами, пользователями, видами экспертизы
- 🎨 **Современный UI**: Tailwind CSS, адаптивный дизайн, тосты и модали

## Структура проекта

```
src/
├── types/              # TypeScript типы
├── api/
│   ├── mockApi.ts      # Mock API с асинхронными вызовами и задержкой
│   └── seed.ts         # Первичные тестовые данные
├── storage/
│   └── StorageAdapter.ts  # Абстракция IndexedDB
├── components/
│   └── AppShell.tsx     # Основной layout с навигацией
├── pages/
│   ├── Dashboard.tsx
│   ├── Documents.tsx    # Список документов с поиском/фильтрами
│   ├── CreateDocument.tsx
│   ├── ViewDocument.tsx # Просмотр с версиями, комментариями, OCR
│   ├── Drafts.tsx
│   ├── Templates.tsx    # Список шаблонов
│   ├── TemplateEditor.tsx # WYSIWYG редактор шаблонов
│   ├── FormFill.tsx     # Использование шаблонов
│   ├── Settings.tsx     # Экспорт/импорт, справочники
│   └── Help.tsx
├── App.tsx             # Роутинг
└── index.css           # Tailwind + глобальные стили
```

## Технологический стек

- **React 18** с TypeScript
- **Vite** - быстрая сборка
- **React Router** - навигация
- **Tailwind CSS** - стили
- **React Hot Toast** - уведомления
- **Lucide React** - иконки
- **IndexedDB** - локальное хранилище
- **Zustand** (опционально) - состояние

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Продакшн сборка
npm run build

# Превью сборки
npm run preview
```

## Использование

### 1. Создание документа

```
Dashboard → Документы → Новый документ
```

Заполните основные метаданные:
- Название
- Тип (договор, дело, соглашение и т.д.)
- Номер дела / договора
- Контрагент
- Другие сведения

### 2. Использование шаблонов

```
Dashboard → Шаблоны → Создать/Редактировать
```

Создайте WYSIWYG шаблон с merge-полями:
```html
<h2>ДОГОВОР ПОСТАВКИ</h2>
<p>Между {{supplier_name}} и {{buyer_name}}</p>
<p>Товар: {{product_name}}</p>
```

При использовании шаблона система автоматически:
- Распознает merge-поля
- Предлагает форму для заполнения
- Создает документ с версией 1

### 3. Управление версиями

Каждое изменение документа создает новую версию:
```
Просмотр документа → История версий
```

Откатитесь на любую версию одним нажатием.

### 4. OCR распознавание

```
Просмотр документа → OCR
```

Распознанный текст сохраняется в документе и участвует в полнотекстовом поиске.

### 5. Экспорт/Импорт

```
Настройки → Резервная копия
```

Экспортируйте всю БД в JSON и восстановите при необходимости.

## Mock API

Mock API имитирует асинхронные вызовы с задержкой (~400ms). Все методы возвращают структуру:

```typescript
interface APIResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
}
```

Легко заменить на реальный backend без изменения компонентов благодаря единому интерфейсу.

## Первичная загрузка (Seed)

При первом запуске приложение автоматически:
- Создает тестовые документы
- Создает шаблоны примеров
- Инициализирует справочники контрагентов и пользователей

## Типы документов

- `contract` - Договор
- `case` - Дело
- `agreement` - Соглашение
- `claim` - Исковое заявление
- `resolution` - Решение
- `other` - Прочее

## Статусы документов

- `draft` - Черновик
- `active` - Активный
- `archived` - В архиве
- `completed` - Завершен

## Виды экспертизы

- `civil` - Гражданское право
- `criminal` - Уголовное право
- `labor` - Трудовое право
- `administrative` - Административное право
- `corporate` - Корпоративное право
- `ip` - Интеллектуальная собственность

## Особенности LocalFirst архитектуры

1. **Автономность**: работает без интернета
2. **Приватность**: данные не покидают браузер
3. **Скорость**: прямой доступ к IndexedDB
4. **Синхронизация**: простая реализация при подключении API

## Расширение функционала

### Подключение реального OCR (Tesseract.js)

```typescript
// src/api/mockApi.ts
async recognizeText(file: File): Promise<APIResponse<string>> {
  if (settings.useTesseract) {
    // Tesseract.js реализация
  } else {
    // Mock
  }
}
```

### Подключение backend

Замените методы в `src/api/mockApi.ts` на вызовы реального API:

```typescript
async listDocuments() {
  // Вместо: const documents = await StorageAdapter.getAllDocuments();
  // Используйте: const res = await fetch('/api/documents');
}
```

## Браузерная совместимость

- Chrome/Edge 60+
- Firefox 55+
- Safari 11+
- Требует поддержку IndexedDB и ES2020+

## Лицензия

MIT

## Разработано для

Платформа документооборота для юристов (до 10 пользователей)
- Учет договоров и дел
- Снижение ошибок
- Централизованное управление документами

  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
